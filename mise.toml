# mise.toml — toolchain version pins (single source of truth for this machine)
# Activated via `eval "$(mise activate zsh)"` in zprofile.
# See https://mise.jdx.dev/

[tools]
node = "24.18.0"
python = "3.11.15"
rust = "1.97.1"
